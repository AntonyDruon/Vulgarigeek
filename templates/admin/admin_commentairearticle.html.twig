{% extends "base.html.twig" %}

{% block title %}Gestion des Commentairesarticle{% endblock %}

{% block body %}
<br>
<hr>
{# <pre>
    {{ dump(comments) }}
</pre> #}
{# <a href="{{ path('admin_new_comment') }}" class=" btn btn-success mb-3"> Ajouter un commentaire</a> #}
<table class="table table-bordered text-center">
<thead>
    <tr>
        {% for colonne in colonnes %}
            <th>{{ colonne }}</th>
            
        {% endfor %}
        <th>Auteur</th>
        <th>Article</th>
        {# <th>Editer</th> #}
        <th>Supprimer</th>
    </tr>

</thead>
<tbody>
{% for commentairearticle in commentairearticle %}
    

<tr>
    <td>{{ commentairearticle.id}}</td>
    <td>{{ commentairearticle.contenu}}</td>
    <td>{{ commentairearticle.datecreation|date("d/m/Y à H:i:s") }}</td>
    <td>{{ commentairearticle.utilisateur.pseudo}}</td>
    <td>{{ commentairearticle.article.titre}}</td>
    {# <td><a href="{{ path('admin_edit_comment', {id:comment.id}) }}"class="btn btn-warning "><i class="fas fa-edit"></i></a></td> #}
    <td><a href="{{ path('delete_commentairearticle', {id:commentairearticle.id}) }}"class="btn btn-danger "><i class="fas fa-trash-alt"></i></a></td>
</tr>
{% endfor %}
</tbody>
</table>
<hr>
<br>




{% endblock %}